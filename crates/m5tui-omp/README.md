# m5tui-omp

OMP integration for m5Tui.

M4 stub: `OmpFrame`, `OmpCodec`, `OmpSession` with a line-oriented text
protocol and `StubOmpSession`. Real JSON-RPC over the `omp --mode rpc`
pipe is deferred to device integration.
