# m5tui-voice

Voice memo / PTT for m5Tui.

M5 stub: `AudioIn`/`AudioOut` traits, `NullIn`/`MemoryIn`/`MemoryOut`,
mono PCM16 WAV codec, `PttState` machine, `VoiceInbox`. Real I2S/ES8311
and NS4150B drivers are deferred to the hardware phase.
