[package]
name = "m5tui-profile"
version = "1.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
publish = false
description = "Connection profile registry for m5Tui (SSH + OMP + jump hosts)."

[lints]
workspace = true

[dependencies]
m5tui-persist = { path = "../m5tui-persist" }
