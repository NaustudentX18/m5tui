# m5tui-profile

Connection profile registry for m5Tui.

M3 stub: `Profile` + `ProfileRegistry` + `InMemoryRegistry`. Real
Tailscale/SSH profile loading from `/sd/m5tui/profiles/` is deferred to
device integration.
