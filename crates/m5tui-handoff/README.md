# m5tui-handoff

Handoff viewer and vault search for m5Tui.

M6 stub: `Handoff`, `HandoffStore`, minimal Markdown renderer, and
`VaultSearch` trait with `StubVaultClient`. Real SFTP fetch and
obsidian-memory integration are deferred.
