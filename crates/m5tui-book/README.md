# m5tui-book

Book of Commands for m5Tui.

M5a stub: `Spell`, `Param`, `BookRegistry`, `InMemoryRegistry`,
template expansion, undo stack. Real YAML loader for `book/*.yaml` and
on-device author mode are deferred. The `book/` directory contains 6
sample spells.
